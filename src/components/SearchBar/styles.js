import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container:{
        justifyContent: 'center',
        alignItems: 'center',
        marginTop:24,
    },
    search:{
        width:'90%',
        height: 52,
        borderColor: 'grey',
        borderWidth:1,
        borderRadius:16,
        justifyContent:'center',
        paddingLeft: 15,
    }
})