import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex:1,
        backgroundColor: '#FFF',
    },

    title:{
        marginLeft: 30,
        fontSize: 20,
        fontWeight: 'bold',
        marginTop: 28,
    },
    
})